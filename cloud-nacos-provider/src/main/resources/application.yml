server:
  port: 10001

spring:
  application:
    name: cloud-nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 暴露端口
management:
  endpoints:
    web:
      exposure:
        include: '*'